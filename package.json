{
  "name": "the-shareit-network",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ michielpost/the-shareit-network",
    "create-local": "graph create --node http://localhost:8020/ michielpost/the-shareit-network",
    "remove-local": "graph remove --node http://localhost:8020/ michielpost/the-shareit-network",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 michielpost/the-shareit-network"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
